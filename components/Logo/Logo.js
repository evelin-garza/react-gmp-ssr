import React from "react";

import styles from "./Logo.module.scss";

const Logo = () => {
    return (
        <span className={styles.logo}><strong>netflix</strong>roulette</span>
    );
};

export default Logo;